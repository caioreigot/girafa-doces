<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.GirafaDoces" parent="Theme.MaterialComponents.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/primaryPink</item>
        <item name="colorPrimaryVariant">@color/primaryPink</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/secondaryPink</item>
        <item name="colorSecondaryVariant">@color/secondaryPinkVariant</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="android:windowBackground">@color/white</item>
        <item name="editTextStyle">@style/MyEditTextStyle</item>
        <item name="android:textViewStyle">@style/MyTextViewStyle</item>
    </style>
    
    <style name="SignUpScreen" parent="Theme.GirafaDoces">
        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:windowFullscreen">true</item>
    </style>

    <!-- Used by Default -->
    <style name="MyEditTextStyle" parent="android:Widget.EditText">
        <item name="android:textColor">@color/primaryBrown</item>
    </style>

    <style name="MyTextViewStyle">
        <item name="android:textColor">@color/black</item>
    </style>

    <!-- Used when passing this style to the component -->
    <style name="LoginEditTextNoPadding">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">@dimen/EditTextHeight</item>
        <item name="android:background">@drawable/login_edit_text_bg</item>
        <item name="android:gravity">center</item>
        <item name="android:scrollHorizontally">true</item>
        <item name="android:singleLine">true</item>
        <item name="android:textColorHint">@color/pinkHintColor</item>
    </style>

    <style name="LoginEditTextWithPadding" parent="LoginEditTextNoPadding">
        <item name="android:paddingStart">45dp</item>
        <item name="android:paddingEnd">45dp</item>
    </style>

    <style name="SignUpEditText">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">@dimen/EditTextHeight</item>
        <item name="android:ems">15</item>
        <item name="android:paddingStart">12dp</item>
        <item name="android:paddingEnd">12dp</item>
        <item name="android:background">@drawable/sign_up_edit_text_bg</item>
        <item name="android:gravity">center</item>
        <item name="android:scrollHorizontally">true</item>
        <item name="android:singleLine">true</item>
        <item name="android:textColorHint">@color/pinkHintColor</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="FullScreenDialog">
        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowFrame">@null</item>
        <item name="android:windowIsFloating">false</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowAnimationStyle">@android:style/Animation.Dialog</item>
        <item name="android:windowSoftInputMode">stateUnspecified|adjustPan</item>
    </style>

    <style name="PinkButton" parent="Base.TextAppearance.AppCompat.Button" >
        <item name="backgroundTint">@color/primaryPink</item>
    </style>

</resources>